//Voice.jsx (app/home/screens/Sites/Voice.jsx)
// Use a relative path to the component (project doesn't currently map `@` to root)
import VoiceScreen from "../../../../components/SearchBar/Voice/voice";

export default VoiceScreen;